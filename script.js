const loader=document.querySelector('.loader');window.addEventListener('load',()=>setTimeout(()=>loader.classList.add('hide'),900));
const menu=document.querySelector('.menu'),nav=document.querySelector('.nav');menu?.addEventListener('click',()=>{nav.classList.toggle('open')});
// Three.js star/particle universe
const canvas=document.getElementById('space');if(canvas&&window.THREE){const scene=new THREE.Scene();const camera=new THREE.PerspectiveCamera(55,innerWidth/innerHeight,.1,1000);camera.position.z=7;const renderer=new THREE.WebGLRenderer({canvas,alpha:true,antialias:true});renderer.setPixelRatio(Math.min(devicePixelRatio,2));renderer.setSize(innerWidth,innerHeight);const geo=new THREE.BufferGeometry();const count=1200;const pos=new Float32Array(count*3);for(let i=0;i<count;i++){pos[i*3]=(Math.random()-.5)*18;pos[i*3+1]=(Math.random()-.5)*11;pos[i*3+2]=(Math.random()-.5)*8}geo.setAttribute('position',new THREE.BufferAttribute(pos,3));const mat=new THREE.PointsMaterial({color:0xb6e46a,size:.018,transparent:true,opacity:.8});const stars=new THREE.Points(geo,mat);scene.add(stars);const sphere=new THREE.Mesh(new THREE.IcosahedronGeometry(1.2,3),new THREE.MeshBasicMaterial({color:0xd7b86a,wireframe:true,transparent:true,opacity:.18}));sphere.position.set(2.1,.1,0);scene.add(sphere);let mx=0,my=0;addEventListener('pointermove',e=>{mx=(e.clientX/innerWidth-.5)*.5;my=(e.clientY/innerHeight-.5)*.3});function animate(){requestAnimationFrame(animate);stars.rotation.y+=.0005;stars.rotation.x+=.00015;sphere.rotation.x+=.002;sphere.rotation.y+=.004;scene.rotation.y+=(mx-scene.rotation.y)*.015;scene.rotation.x+=(-my-scene.rotation.x)*.015;renderer.render(scene,camera)}animate();addEventListener('resize',()=>{camera.aspect=innerWidth/innerHeight;camera.updateProjectionMatrix();renderer.setSize(innerWidth,innerHeight)})}
const services=[...document.querySelectorAll('.service')],no=document.getElementById('detailNo'),title=document.getElementById('detailTitle'),copy=document.getElementById('detailCopy'),modal=document.getElementById('modal');services.forEach((s,i)=>s.addEventListener('click',()=>{services.forEach(x=>x.classList.remove('active'));s.classList.add('active');no.textContent=String(i+1).padStart(2,'0');title.textContent=s.dataset.title;copy.textContent=s.dataset.copy;if(innerWidth<700){modal.classList.add('show');document.getElementById('modalNo').textContent=no.textContent;document.getElementById('modalTitle').textContent=s.dataset.title;document.getElementById('modalCopy').textContent=s.dataset.copy}}));document.getElementById('close')?.addEventListener('click',()=>modal.classList.remove('show'));modal?.addEventListener('click',e=>{if(e.target===modal)modal.classList.remove('show')});
document.querySelectorAll('.nav nav a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));
document.getElementById('contactForm')?.addEventListener('submit',e=>{e.preventDefault();const f=new FormData(e.target);const subject=encodeURIComponent('AuMDS Business Enquiry — '+f.get('name'));const body=encodeURIComponent('Name: '+f.get('name')+'\nEmail: '+f.get('email')+'\n\nRequirement:\n'+f.get('message'));location.href=`mailto:contact@aumdsorg.com?subject=${subject}&body=${body}`});
